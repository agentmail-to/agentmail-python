# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing
import uuid

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.ascending import Ascending
from ..types.limit import Limit
from ..types.page_token import PageToken
from .raw_client import AsyncRawApiKeysClient, RawApiKeysClient
from .types.api_key_id import ApiKeyId
from .types.api_key_permissions import ApiKeyPermissions
from .types.create_api_key_response import CreateApiKeyResponse
from .types.list_api_keys_response import ListApiKeysResponse
from .types.list_public_keys_response import ListPublicKeysResponse
from .types.name import Name
from .types.public_jwk import PublicJwk
from .types.public_key_credential import PublicKeyCredential
from .types.public_key_scope import PublicKeyScope
from .types.revoke_all_agent_id_sign_in_keys_response import RevokeAllAgentIdSignInKeysResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class ApiKeysClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawApiKeysClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawApiKeysClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawApiKeysClient
        """
        return self._raw_client

    def list(
        self,
        *,
        limit: typing.Optional[Limit] = None,
        page_token: typing.Optional[PageToken] = None,
        ascending: typing.Optional[Ascending] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListApiKeysResponse:
        """
        **CLI:**
        ```bash
        agentmail api-keys list
        ```

        Parameters
        ----------
        limit : typing.Optional[Limit]

        page_token : typing.Optional[PageToken]

        ascending : typing.Optional[Ascending]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListApiKeysResponse

        Examples
        --------
        from agentmail import AgentMail

        client = AgentMail(
            api_key="YOUR_API_KEY",
        )
        client.api_keys.list()
        """
        _response = self._raw_client.list(
            limit=limit, page_token=page_token, ascending=ascending, request_options=request_options
        )
        return _response.data

    def create(
        self,
        *,
        name: typing.Optional[Name] = OMIT,
        permissions: typing.Optional[ApiKeyPermissions] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateApiKeyResponse:
        """
        **CLI:**
        ```bash
        agentmail api-keys create --name "My Key"
        ```

        Parameters
        ----------
        name : typing.Optional[Name]

        permissions : typing.Optional[ApiKeyPermissions]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateApiKeyResponse

        Examples
        --------
        from agentmail import AgentMail

        client = AgentMail(
            api_key="YOUR_API_KEY",
        )
        client.api_keys.create()
        """
        _response = self._raw_client.create(name=name, permissions=permissions, request_options=request_options)
        return _response.data

    def delete(self, api_key_id: ApiKeyId, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        **CLI:**
        ```bash
        agentmail api-keys delete --api-key-id <api_key_id>
        ```

        Parameters
        ----------
        api_key_id : ApiKeyId

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from agentmail import AgentMail

        client = AgentMail(
            api_key="YOUR_API_KEY",
        )
        client.api_keys.delete(
            api_key_id="api_key_id",
        )
        """
        _response = self._raw_client.delete(api_key_id, request_options=request_options)
        return _response.data

    def list_public_keys(
        self,
        *,
        limit: typing.Optional[Limit] = None,
        page_token: typing.Optional[PageToken] = None,
        ascending: typing.Optional[Ascending] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListPublicKeysResponse:
        """
        List only public-key credentials visible to the bearer caller's scope.
        Bearer credentials are never returned, even though both credential types
        share storage and pagination indexes. Requires `api_key_read`.

        Parameters
        ----------
        limit : typing.Optional[Limit]

        page_token : typing.Optional[PageToken]

        ascending : typing.Optional[Ascending]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListPublicKeysResponse

        Examples
        --------
        from agentmail import AgentMail

        client = AgentMail(
            api_key="YOUR_API_KEY",
        )
        client.api_keys.list_public_keys()
        """
        _response = self._raw_client.list_public_keys(
            limit=limit, page_token=page_token, ascending=ascending, request_options=request_options
        )
        return _response.data

    def create_public_key(
        self,
        *,
        public_key: PublicJwk,
        name: typing.Optional[str] = OMIT,
        scope: typing.Optional[PublicKeyScope] = OMIT,
        expires_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicKeyCredential:
        """
        Register a public P-256 JWK using an existing AgentMail bearer API key
        with `api_key_create`. Re-registering the same JWK creates a new
        credential ID; it does not replace or recover an earlier credential.
        The private key must never be sent to AgentMail.

        Parameters
        ----------
        public_key : PublicJwk

        name : typing.Optional[str]
            Defaults to `AgentID key {first eight fingerprint characters}`.

        scope : typing.Optional[PublicKeyScope]
            Omit to inherit the registering bearer key's exact scope. An explicit
            scope must be the caller's scope or a live descendant.

        expires_at : typing.Optional[dt.datetime]
            Future absolute expiry. Omit to inherit the registering bearer key's
            expiry. A child credential cannot outlive its creator.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicKeyCredential

        Examples
        --------
        from agentmail import AgentMail
        from agentmail.api_keys import PublicJwk

        client = AgentMail(
            api_key="YOUR_API_KEY",
        )
        client.api_keys.create_public_key(
            public_key=PublicJwk(
                x="blackcurrant...............................",
                y="blackcurrant...............................",
            ),
        )
        """
        _response = self._raw_client.create_public_key(
            public_key=public_key, name=name, scope=scope, expires_at=expires_at, request_options=request_options
        )
        return _response.data

    def update_public_key_name(
        self, api_key_id: uuid.UUID, *, name: str, request_options: typing.Optional[RequestOptions] = None
    ) -> PublicKeyCredential:
        """
        Rename the credential. All security-relevant fields are immutable.
        Requires `api_key_update`.

        Parameters
        ----------
        api_key_id : uuid.UUID
            Public-key credential ID returned by registration.

        name : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicKeyCredential

        Examples
        --------
        import uuid

        from agentmail import AgentMail

        client = AgentMail(
            api_key="YOUR_API_KEY",
        )
        client.api_keys.update_public_key_name(
            api_key_id=uuid.UUID(
                "d5e9c84f-c2b2-4bf4-b4b0-7ffd7a9ffc32",
            ),
            name="x",
        )
        """
        _response = self._raw_client.update_public_key_name(api_key_id, name=name, request_options=request_options)
        return _response.data

    def revoke_public_key(
        self, api_key_id: uuid.UUID, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Permanently revoke one public-key credential. This hard-deletes the
        credential; repeating the request returns not found. Requires
        `api_key_delete`.

        Parameters
        ----------
        api_key_id : uuid.UUID
            Public-key credential ID returned by registration.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import uuid

        from agentmail import AgentMail

        client = AgentMail(
            api_key="YOUR_API_KEY",
        )
        client.api_keys.revoke_public_key(
            api_key_id=uuid.UUID(
                "d5e9c84f-c2b2-4bf4-b4b0-7ffd7a9ffc32",
            ),
        )
        """
        _response = self._raw_client.revoke_public_key(api_key_id, request_options=request_options)
        return _response.data

    def revoke_all_agent_id_sign_in_keys(
        self, *, idempotency_key: str, request_options: typing.Optional[RequestOptions] = None
    ) -> RevokeAllAgentIdSignInKeysResponse:
        """
        Invalidate every current public-key credential in the caller's
        organization by advancing its AgentID key generation. The caller must be
        organization-scoped and either have `api_key_delete` or, for a verified
        self-serve agent organization, use an unrestricted unmanaged bearer
        credential. No request body is accepted.

        `Idempotency-Key` is required and must be a UUID. Reusing the same UUID
        returns the original permanent receipt without advancing the generation
        again. A new UUID performs a new generation advance.

        Parameters
        ----------
        idempotency_key : str
            Required UUID identifying this revoke-all operation permanently.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RevokeAllAgentIdSignInKeysResponse

        Examples
        --------
        from agentmail import AgentMail

        client = AgentMail(
            api_key="YOUR_API_KEY",
        )
        client.api_keys.revoke_all_agent_id_sign_in_keys(
            idempotency_key="Idempotency-Key",
        )
        """
        _response = self._raw_client.revoke_all_agent_id_sign_in_keys(
            idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data


class AsyncApiKeysClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawApiKeysClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawApiKeysClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawApiKeysClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        limit: typing.Optional[Limit] = None,
        page_token: typing.Optional[PageToken] = None,
        ascending: typing.Optional[Ascending] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListApiKeysResponse:
        """
        **CLI:**
        ```bash
        agentmail api-keys list
        ```

        Parameters
        ----------
        limit : typing.Optional[Limit]

        page_token : typing.Optional[PageToken]

        ascending : typing.Optional[Ascending]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListApiKeysResponse

        Examples
        --------
        import asyncio

        from agentmail import AsyncAgentMail

        client = AsyncAgentMail(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.api_keys.list()


        asyncio.run(main())
        """
        _response = await self._raw_client.list(
            limit=limit, page_token=page_token, ascending=ascending, request_options=request_options
        )
        return _response.data

    async def create(
        self,
        *,
        name: typing.Optional[Name] = OMIT,
        permissions: typing.Optional[ApiKeyPermissions] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateApiKeyResponse:
        """
        **CLI:**
        ```bash
        agentmail api-keys create --name "My Key"
        ```

        Parameters
        ----------
        name : typing.Optional[Name]

        permissions : typing.Optional[ApiKeyPermissions]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateApiKeyResponse

        Examples
        --------
        import asyncio

        from agentmail import AsyncAgentMail

        client = AsyncAgentMail(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.api_keys.create()


        asyncio.run(main())
        """
        _response = await self._raw_client.create(name=name, permissions=permissions, request_options=request_options)
        return _response.data

    async def delete(self, api_key_id: ApiKeyId, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        **CLI:**
        ```bash
        agentmail api-keys delete --api-key-id <api_key_id>
        ```

        Parameters
        ----------
        api_key_id : ApiKeyId

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from agentmail import AsyncAgentMail

        client = AsyncAgentMail(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.api_keys.delete(
                api_key_id="api_key_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(api_key_id, request_options=request_options)
        return _response.data

    async def list_public_keys(
        self,
        *,
        limit: typing.Optional[Limit] = None,
        page_token: typing.Optional[PageToken] = None,
        ascending: typing.Optional[Ascending] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListPublicKeysResponse:
        """
        List only public-key credentials visible to the bearer caller's scope.
        Bearer credentials are never returned, even though both credential types
        share storage and pagination indexes. Requires `api_key_read`.

        Parameters
        ----------
        limit : typing.Optional[Limit]

        page_token : typing.Optional[PageToken]

        ascending : typing.Optional[Ascending]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListPublicKeysResponse

        Examples
        --------
        import asyncio

        from agentmail import AsyncAgentMail

        client = AsyncAgentMail(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.api_keys.list_public_keys()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_public_keys(
            limit=limit, page_token=page_token, ascending=ascending, request_options=request_options
        )
        return _response.data

    async def create_public_key(
        self,
        *,
        public_key: PublicJwk,
        name: typing.Optional[str] = OMIT,
        scope: typing.Optional[PublicKeyScope] = OMIT,
        expires_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicKeyCredential:
        """
        Register a public P-256 JWK using an existing AgentMail bearer API key
        with `api_key_create`. Re-registering the same JWK creates a new
        credential ID; it does not replace or recover an earlier credential.
        The private key must never be sent to AgentMail.

        Parameters
        ----------
        public_key : PublicJwk

        name : typing.Optional[str]
            Defaults to `AgentID key {first eight fingerprint characters}`.

        scope : typing.Optional[PublicKeyScope]
            Omit to inherit the registering bearer key's exact scope. An explicit
            scope must be the caller's scope or a live descendant.

        expires_at : typing.Optional[dt.datetime]
            Future absolute expiry. Omit to inherit the registering bearer key's
            expiry. A child credential cannot outlive its creator.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicKeyCredential

        Examples
        --------
        import asyncio

        from agentmail import AsyncAgentMail
        from agentmail.api_keys import PublicJwk

        client = AsyncAgentMail(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.api_keys.create_public_key(
                public_key=PublicJwk(
                    x="blackcurrant...............................",
                    y="blackcurrant...............................",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_public_key(
            public_key=public_key, name=name, scope=scope, expires_at=expires_at, request_options=request_options
        )
        return _response.data

    async def update_public_key_name(
        self, api_key_id: uuid.UUID, *, name: str, request_options: typing.Optional[RequestOptions] = None
    ) -> PublicKeyCredential:
        """
        Rename the credential. All security-relevant fields are immutable.
        Requires `api_key_update`.

        Parameters
        ----------
        api_key_id : uuid.UUID
            Public-key credential ID returned by registration.

        name : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicKeyCredential

        Examples
        --------
        import asyncio
        import uuid

        from agentmail import AsyncAgentMail

        client = AsyncAgentMail(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.api_keys.update_public_key_name(
                api_key_id=uuid.UUID(
                    "d5e9c84f-c2b2-4bf4-b4b0-7ffd7a9ffc32",
                ),
                name="x",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_public_key_name(
            api_key_id, name=name, request_options=request_options
        )
        return _response.data

    async def revoke_public_key(
        self, api_key_id: uuid.UUID, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Permanently revoke one public-key credential. This hard-deletes the
        credential; repeating the request returns not found. Requires
        `api_key_delete`.

        Parameters
        ----------
        api_key_id : uuid.UUID
            Public-key credential ID returned by registration.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio
        import uuid

        from agentmail import AsyncAgentMail

        client = AsyncAgentMail(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.api_keys.revoke_public_key(
                api_key_id=uuid.UUID(
                    "d5e9c84f-c2b2-4bf4-b4b0-7ffd7a9ffc32",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.revoke_public_key(api_key_id, request_options=request_options)
        return _response.data

    async def revoke_all_agent_id_sign_in_keys(
        self, *, idempotency_key: str, request_options: typing.Optional[RequestOptions] = None
    ) -> RevokeAllAgentIdSignInKeysResponse:
        """
        Invalidate every current public-key credential in the caller's
        organization by advancing its AgentID key generation. The caller must be
        organization-scoped and either have `api_key_delete` or, for a verified
        self-serve agent organization, use an unrestricted unmanaged bearer
        credential. No request body is accepted.

        `Idempotency-Key` is required and must be a UUID. Reusing the same UUID
        returns the original permanent receipt without advancing the generation
        again. A new UUID performs a new generation advance.

        Parameters
        ----------
        idempotency_key : str
            Required UUID identifying this revoke-all operation permanently.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RevokeAllAgentIdSignInKeysResponse

        Examples
        --------
        import asyncio

        from agentmail import AsyncAgentMail

        client = AsyncAgentMail(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.api_keys.revoke_all_agent_id_sign_in_keys(
                idempotency_key="Idempotency-Key",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.revoke_all_agent_id_sign_in_keys(
            idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data
