# This file was auto-generated by Fern from our API Definition.

import typing
from json.decoder import JSONDecodeError

from ...api_keys.types.browser_enrollment_accepted import BrowserEnrollmentAccepted
from ...api_keys.types.browser_enrollment_transaction_jti import BrowserEnrollmentTransactionJti
from ...core.api_error import ApiError
from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.http_response import AsyncHttpResponse, HttpResponse
from ...core.jsonable_encoder import jsonable_encoder
from ...core.parse_error import ParsingError
from ...core.request_options import RequestOptions
from ...core.unchecked_base_model import construct_type
from ...errors.conflict_error import ConflictError
from ...errors.not_found_error import NotFoundError
from ...errors.validation_error import ValidationError as errors_validation_error_ValidationError
from ...types.error_response import ErrorResponse
from ...types.validation_error_response import ValidationErrorResponse
from ..types.inbox_id import InboxId
from pydantic import ValidationError as pydantic_ValidationError

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class RawBrowserCredentialsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._client_wrapper = client_wrapper

    def create_enrollment(
        self,
        inbox_id: InboxId,
        *,
        transaction_jti: BrowserEnrollmentTransactionJti,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[BrowserEnrollmentAccepted]:
        """
        Attach a browser enrollment intent to the inbox. Requires
        `api_key_create`. Before submitting `transaction_jti`, independently
        verify that the browser page's final origin is exactly
        `https://auth.agentid.com`.

        This endpoint is available to every organization using US production.
        It is not available in EU production.

        Select `inbox_id` from trusted AgentMail configuration. An AgentID
        `login_hint` is not authoritative for selecting the inbox; when the
        transaction includes one, it must match the path inbox.

        **AgentMail API keys are sent only to `https://api.agentmail.to`; AgentID never requests them.**

        A new intent returns `202`; an idempotent retry for the same pending
        transaction, inbox, and bearer key returns `200` with the same receipt.
        An intent lasts at most five minutes. An activated credential lasts at
        most 30 days and cannot outlive its authorizing bearer API key.

        Creation is limited to 20 intents per bearer API key per hour, 100 per
        organization per hour, and five live unused intents per bearer API key.
        Browser activation is separately limited to 20 activations per
        authorizing bearer API key per UTC day. Either kind of limit can return
        `429`; honor the `Retry-After` header. Cancelling an enrollment releases
        its live-intent slot but does not reset the daily activation counter.

        Parameters
        ----------
        inbox_id : InboxId

        transaction_jti : BrowserEnrollmentTransactionJti

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[BrowserEnrollmentAccepted]
        """
        _response = self._client_wrapper.httpx_client.request(
            f"v0/inboxes/{jsonable_encoder(inbox_id)}/browser-credentials/enrollments",
            base_url=self._client_wrapper.get_environment().http,
            method="POST",
            json={
                "transaction_jti": transaction_jti,
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    BrowserEnrollmentAccepted,
                    construct_type(
                        type_=BrowserEnrollmentAccepted,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise errors_validation_error_ValidationError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        ValidationErrorResponse,
                        construct_type(
                            type_=ValidationErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        ErrorResponse,
                        construct_type(
                            type_=ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        ErrorResponse,
                        construct_type(
                            type_=ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except pydantic_ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)


class AsyncRawBrowserCredentialsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._client_wrapper = client_wrapper

    async def create_enrollment(
        self,
        inbox_id: InboxId,
        *,
        transaction_jti: BrowserEnrollmentTransactionJti,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[BrowserEnrollmentAccepted]:
        """
        Attach a browser enrollment intent to the inbox. Requires
        `api_key_create`. Before submitting `transaction_jti`, independently
        verify that the browser page's final origin is exactly
        `https://auth.agentid.com`.

        This endpoint is available to every organization using US production.
        It is not available in EU production.

        Select `inbox_id` from trusted AgentMail configuration. An AgentID
        `login_hint` is not authoritative for selecting the inbox; when the
        transaction includes one, it must match the path inbox.

        **AgentMail API keys are sent only to `https://api.agentmail.to`; AgentID never requests them.**

        A new intent returns `202`; an idempotent retry for the same pending
        transaction, inbox, and bearer key returns `200` with the same receipt.
        An intent lasts at most five minutes. An activated credential lasts at
        most 30 days and cannot outlive its authorizing bearer API key.

        Creation is limited to 20 intents per bearer API key per hour, 100 per
        organization per hour, and five live unused intents per bearer API key.
        Browser activation is separately limited to 20 activations per
        authorizing bearer API key per UTC day. Either kind of limit can return
        `429`; honor the `Retry-After` header. Cancelling an enrollment releases
        its live-intent slot but does not reset the daily activation counter.

        Parameters
        ----------
        inbox_id : InboxId

        transaction_jti : BrowserEnrollmentTransactionJti

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[BrowserEnrollmentAccepted]
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"v0/inboxes/{jsonable_encoder(inbox_id)}/browser-credentials/enrollments",
            base_url=self._client_wrapper.get_environment().http,
            method="POST",
            json={
                "transaction_jti": transaction_jti,
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    BrowserEnrollmentAccepted,
                    construct_type(
                        type_=BrowserEnrollmentAccepted,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            if _response.status_code == 400:
                raise errors_validation_error_ValidationError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        ValidationErrorResponse,
                        construct_type(
                            type_=ValidationErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 404:
                raise NotFoundError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        ErrorResponse,
                        construct_type(
                            type_=ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            if _response.status_code == 409:
                raise ConflictError(
                    headers=dict(_response.headers),
                    body=typing.cast(
                        ErrorResponse,
                        construct_type(
                            type_=ErrorResponse,  # type: ignore
                            object_=_response.json(),
                        ),
                    ),
                )
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except pydantic_ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)
